# Project Visibility follows the user, not the repository

Which Skills a workspace uses is personal. SkillHub stores Project Visibility on the user/machine, keyed by folder, and does not write it into the git repo. Clone the repo on another machine and you get Global until this user sets Project again.
