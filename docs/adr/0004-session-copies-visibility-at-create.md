# A Session copies Visibility when it is created

The default rule is snapshot. Opening a Session copies whatever Global and Project currently resolve to. Toggles in that Session stay there. Changing Project or Global later does not rewrite open Sessions. New Sessions pick up the new defaults. The settings UI still lets the user edit Global, Project, or this Session on purpose.
